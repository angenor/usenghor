---

description: "Liste des tâches d'implémentation — 023 Mini-site public PEI : accueil et « Nos activités »"
---

# Tasks: Mini-site public « Entreprendre à Senghor » — accueil et « Nos activités »

**Input**: Design documents from `/specs/023-pei-public-home-activities/`

**Prerequisites**: plan.md, spec.md (Clarifications Q1–Q5), research.md (R1–R17), data-model.md (SQL 047 § 3, filtre événements § 4), contracts/ (public-api, editorial-keys, frontend), quickstart.md

**Tests**: Un test backend `pytest` pour le filtre public des événements (plan.md « Testing », research R15). Aucune infrastructure de test frontend : validation manuelle par quickstart.md + `pnpm lint` + `pnpm build` + Lighthouse + captures avant / après.

**Organization**: Tâches groupées par user story (spec.md) : US1 accueil `/entrepreneuriat` (P1) → US2 « Nos activités » (P2) → US3 sous-navigation et slider accessibles (P2) → US4 trilingue, RTL, mobile, sombre, SEO (P3). Les briques partagées (hero étendu, sous-nav, carte d'actualité, composables, i18n, migration 047, filtre événements) sont en phase 2 car les deux pages en dépendent.

## Format: `[ID] [P?] [Story] Description`

- **[P]** : parallélisable (fichiers différents, aucune dépendance sur une tâche inachevée)
- **[Story]** : US1…US4 (spec.md)
- Chemins exacts depuis la racine du dépôt ; `BE` = `usenghor_backend/`, `FE` = `usenghor_nuxt/app/`, `I18N` = `usenghor_nuxt/i18n/locales/`, `SQL` = `usenghor_backend/documentation/modele_de_données/`

## Path Conventions

Monorepo : backend `usenghor_backend/app/{routers/public,services}` + `tests/integration` ; frontend `usenghor_nuxt/app/{pages/entrepreneuriat,components/entrepreneurship,components/page,components/actualites,composables,utils,types/api,assets/css}` + `usenghor_nuxt/i18n/locales/{fr,en,ar}`. Composants auto-importés par chemin (`components/entrepreneurship/SubNav.vue` → `<EntrepreneurshipSubNav>`, `components/actualites/NewsCard.vue` → `<ActualitesNewsCard>`). Libellés fixes sous la racine i18n `pei` (R12), copie éditoriale via `getRawContent` (R6), données trilingues via `useLocalizedField().localized(obj, base)`, propriétés logiques Tailwind (`ms-`, `me-`, `ps-`, `pe-`, `start-`, `end-`) et classes `dark:` partout (R16).

---

## Phase 1: Setup (porte SQL et socle transverse sans dépendance)

**Purpose**: Obtenir l'accord sur la seule migration de la feature et poser les fichiers partagés qui ne dépendent de rien.

- [ ] T001 **Porte FR-029** — Présenter au responsable du projet le SQL de `specs/023-pei-public-home-activities/data-model.md` § 3 (migration `047_pei_activities_hero_keys.sql` : 4 lignes `editorial_contents`, `value_type = 'text'`, catégorie `values`, `ON CONFLICT (key) DO NOTHING` ; rollback `DELETE … WHERE key IN (…)`) ; ne commencer T014 qu'après accord explicite et reporter dans data-model.md toute modification demandée. Toutes les autres tâches peuvent démarrer sans attendre. — **2026-09-13 : accord explicite non recueilli.** Fichiers 047 créés et rejoués en local uniquement (réversibles) ; ne pas jouer en production sans accord.
- [X] T002 [P] Ajouter l'utilitaire global `.scrollbar-hide` dans `FE/assets/css/main.css` (`-ms-overflow-style: none; scrollbar-width: none;` + `.scrollbar-hide::-webkit-scrollbar { display: none; }`) sans toucher aux copies `<style scoped>` existantes (R2).
- [X] T003 [P] Créer `FE/utils/pei-presentation.ts` (auto-import Nuxt) exportant : `PEI_PHASE_ORDER: PeiProgramPhase[] = ['awareness','status','pre_incubation','incubation','funding','ecosystem']` ; `PEI_PHASE_ANCHORS: Record<PeiProgramPhase, string>` (`phase-awareness`, `phase-status`, `phase-pre-incubation`, `phase-incubation`, `phase-funding`, `phase-ecosystem`) ; `PEI_FAMILY_ORDER: PeiPartnerFamily[] = ['academic','support','international']` ; `PEI_COLOR_CLASSES: Record<PeiColor, { border: string; numBg: string; numText: string; label: string }>` avec les classes de research R13 (bleu `border-t-brand-blue-500` / `bg-brand-blue-100 dark:bg-brand-blue-900/40` / `text-brand-blue-700 dark:text-brand-blue-300` ; bleu foncé `border-t-brand-blue-700` / `bg-brand-blue-100 dark:bg-brand-blue-950/50` / `text-brand-blue-800 dark:text-brand-blue-200` ; rouge `border-t-brand-red-500` / `bg-brand-red-100 dark:bg-brand-red-900/40` / `text-brand-red-700 dark:text-brand-red-300` ; ambre `border-t-amber-500` / `bg-amber-100 dark:bg-amber-900/40` / `text-amber-800 dark:text-amber-300` ; turquoise `border-t-teal-600` / `bg-teal-100 dark:bg-teal-900/40` / `text-teal-800 dark:text-teal-300`) ; `peiColorClasses(color: string)` renvoyant `PEI_COLOR_CLASSES[color] ?? PEI_COLOR_CLASSES.blue` ; `isNumericStat(value: string): boolean` (`/^\d+[^\d]*$/`). Types importés de `FE/types/api/entrepreneurship.ts`.
- [X] T004 [P] Créer `I18N/fr/entrepreneurship.json` (racine `pei`) avec toutes les clés de `contracts/frontend.md` § i18n : `pei.nav.{presentation,activities,alumni,partners,resources,news,cta,label}`, `pei.breadcrumb.{dde,pole}`, `pei.phases.{awareness,status,pre_incubation,incubation,funding,ecosystem}`, `pei.families.{academic,support,international}`, `pei.home.{ourNews,allNews,ourPartners,partnersBadge,newsBadge,discoverActivities}`, `pei.activities.{anchorsLabel,upcomingEvents,online,eventLink}`, `pei.seo.{homeTitle,homeDescription,activitiesTitle,activitiesDescription}` (textes FR du cahier des charges / maquette, accents obligatoires) ; l'enregistrer dans `I18N/fr/index.ts` (`import entrepreneurship from './entrepreneurship.json'` + `...entrepreneurship`).
- [X] T005 [P] Créer `I18N/en/entrepreneurship.json` (mêmes clés, traductions anglaises : « Overview », « Our activities », « Our alumni », « Our partners », « Our resources », « News », « Become a student-entrepreneur », phases « Awareness », « Framework », « Pre-incubation », « Incubation », « Seed funding », « Ecosystem animation », familles « Academic and institutional », « Support organisations », « International organisations », etc.) et l'enregistrer dans `I18N/en/index.ts`.
- [X] T006 [P] Créer `I18N/ar/entrepreneurship.json` (mêmes clés, traductions arabes) et l'enregistrer dans `I18N/ar/index.ts`.

---

## Phase 2: Foundational (briques partagées par les deux pages)

**Purpose**: Backend (filtre événements), composables publics, hero étendu, sous-navigation, carte d'actualité, clés éditoriales 047. Aucune page ne peut être livrée sans cette phase.

**⚠️ CRITICAL**: US1 et US2 consomment tous ces éléments ; US3 et US4 les valident.

### Backend — filtre public des événements (contracts/public-api.md, data-model.md § 4)

- [X] T007 Étendre `ContentService.get_events` dans `BE/app/services/content_service.py` (l. ~328-366) : paramètres `service_id: str | None = None` et `order: str = "desc"` ; après le bloc `campus_id`, `if service_id: query = query.where(Event.service_external_id == service_id)` ; remplacer `order_by(Event.start_date.desc())` par `Event.start_date.asc() if order == "asc" else Event.start_date.desc()`. Aucun autre appelant ne change de comportement (défauts identiques).
- [X] T008 Étendre `list_events` dans `BE/app/routers/public/events.py` : `service_id: str | None = Query(None, description="Filtrer par service (UUID)")`, `order: Literal["asc", "desc"] = Query("desc", description="Tri sur la date de début")` (import `Literal` de `typing`), transmis à `service.get_events(..., service_id=service_id, order=order)` ; vérifier Swagger `/api/docs` (422 sur `order=foo`).
- [X] T009 Créer `BE/tests/integration/test_public_events_service_filter.py` (docstring référençant `specs/023-pei-public-home-activities/contracts/public-api.md`, `BASE = "/api/public/events"`, helper `_seed_events(db)` créant 3 événements publiés — 2 rattachés au service A dont 1 passé, 1 au service B — et 1 brouillon du service A) avec les tests : filtre `service_id=A` → 2 ; `service_id=A&upcoming=true` → 1 ; `order=asc` → dates croissantes et défaut décroissant ; brouillon absent ; `order=foo` → 422. Lancer `pytest tests/integration/test_public_events_service_filter.py -v` jusqu'au vert.

### Frontend — composables

- [X] T010 [P] Étendre `listPublishedEvents(params)` dans `FE/composables/usePublicEventsApi.ts` : ajouter `service_id?: string` et `order?: 'asc' | 'desc'` au type des paramètres et deux lignes `if (params.service_id) query.service_id = params.service_id` / `if (params.order) query.order = params.order` sur le modèle de `campus_id` ; `getUpcomingEvents` inchangé.
- [X] T011 [P] Créer `FE/composables/usePublicEntrepreneurshipApi.ts` sur le modèle de `usePublicOrganizationApi.ts` (`const apiBase = useApiBase()`, `$fetch<T>(\`${apiBase}${BASE}…\`)`, `BASE = '/api/public/entrepreneurship'`, aucun en-tête d'auth) exposant `listPrograms(): Promise<PeiProgramPublic[]>`, `getProgram(code)`, `listCohorts(type?: PeiCohortType)`, `getCohort(code)`, `listLaureates(type?: PeiLaureateType): Promise<PeiLaureatesPublic>`, `listPartners(): Promise<PeiPartnerFamilyPublic[]>`, `listResources(params?: { type?: PeiResourceType; category?: string })`, types importés de `FE/types/api/entrepreneurship.ts` ; erreurs propagées (les pages les absorbent, R9).

### Frontend — hero étendu (research R1, contracts/frontend.md § PageHero)

- [X] T012 Étendre `FE/components/page/Hero.vue` : props optionnelles `images?: string[]`, `badge?: string`, `badgeIcon?: string` (interface `Props` existante, `image` reste prioritaire : `const slides = computed(() => props.image ? [] : (props.images ?? []).filter(Boolean))`, `hasSlider = slides.length >= 2`, `singleImage = props.image ?? (slides.length === 1 ? slides[0] : undefined)`) ; badge rendu au-dessus du `h1` **uniquement si `badge`** (`<span class="inline-flex items-center gap-2 mb-4 rounded-full border border-brand-red-400/30 bg-brand-red-500/20 px-4 py-1.5 text-xs font-semibold uppercase tracking-wider text-brand-red-300">` + `font-awesome-icon` si `badgeIcon`) ; slider : conteneur `absolute inset-0` avec `<TransitionGroup name="slide">` d'`<img>` natifs en `v-show="index === currentSlide"` (`class="absolute inset-0 w-full h-full object-cover"`, première image `fetchpriority="high" decoding="async"`, suivantes `loading="lazy"`, `alt` = `title`), overlay `bg-gray-900/60` conservé ; état `currentSlide`, `goToSlide(i)`, intervalle 6 000 ms démarré `onMounted` et nettoyé `onUnmounted` ; `reducedMotion = matchMedia('(prefers-reduced-motion: reduce)').matches` → pas d'intervalle et classe `slide-static` (transitions désactivées) ; pause / reprise sur `mouseenter` / `mouseleave` / `focusin` / `focusout` de la `<section>` et sur `document.visibilitychange` ; DOM strictement identique quand ni `images` ni `badge` ne sont fournis (mode image / motif inchangés, séparateur oblique conservé) ; gestionnaire `@error` sur chaque `<img>` du slider retirant l'adresse fautive de la liste réactive des slides (recalcul de `hasSlider` → image simple, puis motif, sans erreur console) ; styles `.slide-enter-active/.slide-leave-active { transition: opacity 1s ease-out }` + `.slide-enter-from/.slide-leave-to { opacity: 0 }` en `<style scoped>`.
- [X] T013 Ajouter à `FE/components/page/Hero.vue` les points de navigation accessibles (rendus si `hasSlider`) : `<div class="absolute bottom-8 inset-x-0 z-20 flex justify-center gap-3" role="tablist" :aria-label="t('hero.slider.slides')" @keydown="onDotsKeydown">` avec `<button v-for type="button" role="tab" :aria-selected="index === currentSlide" :tabindex="index === currentSlide ? 0 : -1" :aria-label="t('hero.slider.slide', { n: index + 1, total })" class="h-3 rounded-full transition-all duration-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-white" :class="index === currentSlide ? 'w-12 bg-brand-blue-500' : 'w-3 bg-white/40 hover:bg-white/60'" @click="goToSlide(index, true)">` ; `onDotsKeydown` gère `ArrowLeft` / `ArrowRight` (inversés en RTL via `document.documentElement.dir`), `Home`, `End` (déplace le focus et le visuel) ; région `<p class="sr-only" aria-live="polite">{{ t('hero.slider.current', { n, total }) }}</p>` ; `const { t } = useI18n()` ajouté au script ; ajouter les clés `hero.slider.{slides,slide,current}` dans `I18N/fr/hero.json`, `I18N/en/hero.json`, `I18N/ar/hero.json` (composant transverse → namespace `hero`, pas `pei`).
- [X] T014 [P] Retirer les attributs orphelins (clarification Q3) : dans `FE/pages/a-propos/partenaires/index.vue` (l. ~36-42) supprimer `:badge="getContent('partners.hero.badge')"` et `badge-icon="fa-solid fa-globe"` ; dans `FE/pages/a-propos/organisation/[type]/[slug].vue` (l. ~451-465) supprimer le bloc `<template #badge>…</template>` et refermer `<PageHero … />` ; vérifier qu'aucune variable ne devient inutilisée (`pnpm lint`).

### Frontend — sous-navigation du pôle (research R2)

- [X] T015 Créer `FE/components/entrepreneurship/SubNav.vue` (`<EntrepreneurshipSubNav>`, aucune prop) calqué sur `FE/components/section/about/TabsNav.vue` : racine `<div class="sticky top-20 z-40">`, `<nav :aria-label="t('pei.nav.label')" class="bg-white dark:bg-gray-900 border-b border-gray-200 dark:border-gray-700 shadow-sm">`, conteneur `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8` > `<div class="flex items-center gap-2 overflow-x-auto scrollbar-hide -mx-4 px-4 sm:mx-0 sm:px-0">` ; `tabs` computed = 6 entrées `{ key, to, icon, exact? }` (Présentation `/entrepreneuriat` `fa-solid fa-circle-info` exact ; activités `/entrepreneuriat/activites` `fa-route` ; alumni `/entrepreneuriat/alumni` `fa-user-graduate` ; partenaires `/entrepreneuriat/partenaires` `fa-handshake` ; ressources `/entrepreneuriat/ressources` `fa-toolbox` ; actualités `/entrepreneuriat/actualites` `fa-newspaper`), libellés `t('pei.nav.' + key)` ; `NuxtLink` avec classes actif / inactif recopiées de `TabsNav` (`border-b-2 border-brand-blue-500 text-brand-blue-600 dark:text-brand-blue-400` / `border-transparent text-gray-500 …`), `isActive(to, exact)` recopiée (`route.path` vs `localePath(to)`, `startsWith(path + '/')`), `aria-current="page"` sur l'actif ; bouton `NuxtLink` `/entrepreneuriat/statut-etudiant-entrepreneur` `ms-auto shrink-0 inline-flex items-center gap-2 rounded-full bg-brand-red-500 hover:bg-brand-red-600 px-4 py-2 text-sm font-semibold text-white` avec `fa-solid fa-rocket`, libellé `t('pei.nav.cta')`.

### Frontend — carte d'actualité partagée (research R3)

- [X] T016 Créer `FE/components/actualites/NewsCard.vue` (`<ActualitesNewsCard>`) en extrayant **à l'identique** le markup de la boucle `latestNews` de `FE/pages/actualites/index.vue` (l. ~355-420 : `NuxtLink` vers `/actualites/{slug}` `group block`, `overflow-hidden rounded-xl` + `<img class="w-full h-48 object-cover transition-transform duration-500 group-hover:scale-105" loading="lazy">` ou substitut `fa-solid fa-newspaper`, titre `h3 text-lg font-bold … line-clamp-2`, badges d'association `sector_name` / `service_names` / `project_name`, résumé `line-clamp-2`, date `text-xs`) ; props `item: NewsDisplay`, `showAssociations?: boolean` (défaut `true`), `imageVariant?: 'low' | 'medium'` (défaut `'low'`) ; helpers internes `getCoverImageUrl` (via `useMediaApi().getMediaUrl` + `getImageVariantUrl`), `localized`, `formatDate` (`ar-EG` / `en-US` / `fr-FR`, `{ day: 'numeric', month: 'long', year: 'numeric' }`), `hasAssociations`.
- [X] T017 Remplacer la boucle de `FE/pages/actualites/index.vue` par `<ActualitesNewsCard v-for="item in latestNews" :key="item.id" :item="item" />` en conservant la grille `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6` et en supprimant uniquement les helpers devenus inutilisés ; capturer la page avant / après à 1440 px et 390 px (`pnpm dev`, `/actualites`) et vérifier un diff visuel nul (SC-005).

### Migration 047 et configuration éditoriale (après T001)

- [X] T018 Créer `SQL/migrations/047_pei_activities_hero_keys.sql` en recopiant data-model.md § 3 (en-tête commenté : contexte feature 023, dépend de 045, rejouable ; `BEGIN;` ; `INSERT INTO editorial_contents (key, value, value_type, category_id, description) SELECT … FROM (VALUES (…4 lignes…)) AS v(key, value, value_type, description) ON CONFLICT (key) DO NOTHING;` ; `COMMIT;` ; `\echo 'Migration 047_pei_activities_hero_keys terminée'`) et `SQL/migrations/047_pei_activities_hero_keys_rollback.sql` (`DELETE FROM editorial_contents WHERE key IN (…4 clés…)`). Nom de fichier sans accent.
- [X] T019 Jouer la migration deux fois en local (`docker exec -i usenghor_postgres psql -U usenghor -d usenghor < SQL/migrations/047_pei_activities_hero_keys.sql`), vérifier `SELECT count(*) FROM editorial_contents WHERE key LIKE 'entrepreneurship.%'` = 47 après chaque passage, modifier `entrepreneurship.activities.hero.title` en base puis rejouer (valeur conservée), jouer le rollback (43) puis rejouer (47) — quickstart § 1.
- [X] T020 [P] Ajouter les 4 littéraux `'entrepreneurship.activities.hero.badge' | '…hero.title' | '…hero.subtitle' | '…hero.image'` à l'union `ValueSectionKey` de `FE/types/api/editorial.ts`, à la suite du bloc `entrepreneurship.activities.*` (l. ~606-611).
- [X] T021 [P] Compléter la section `entrepreneurship-activities` de `FE/composables/editorial-pages-config.ts` (l. ~947-969) : 4 clés dans `editorialKeys` et 4 entrées `fields` (`key` = `editorialKey`, `editable: true`, `label` « Badge du hero », « Titre du hero », « Sous-titre du hero » (`type: 'textarea'`), « Image du hero » (`type: 'image'`, `defaultValue: ''`), `description` de contracts/editorial-keys.md, `defaultValue` = valeurs initiales FR) ; vérifier dans Admin → Valeurs → Entrepreneuriat que les 4 champs apparaissent et qu'une image se choisit via le sélecteur de médiathèque.

**Checkpoint**: backend filtré, composables prêts, hero extensible, sous-nav, carte d'actualité, 47 clés éditoriales — les pages peuvent être construites.

---

## Phase 3: User Story 1 — Découvrir le pôle depuis la page d'accueil (Priority: P1) 🎯 MVP

**Goal**: `/entrepreneuriat` rend en SSR les huit sections de la maquette avec les données du backoffice, chaque section masquée proprement si vide.

**Independent Test**: quickstart § 3 (données) puis § 4 : `curl -s http://localhost:3000/entrepreneuriat | grep -c "INNOVER"` ≥ 1 ; toutes les sections visibles dans l'ordre ; modification d'un chiffre clé et d'un titre de dispositif visible ≤ 60 s après ; fil d'Ariane « DDE » → fiche service ; « Découvrir le parcours » → `#parcours` ; carte → `/entrepreneuriat/activites#phase-…`.

### Composants du pôle (contracts/frontend.md § Composants)

- [X] T022 [P] [US1] Créer `FE/components/entrepreneurship/StatsPanel.vue` (`<EntrepreneurshipStatsPanel>`) : props `title: string`, `stats: { value: string; label: string }[]` ; panneau `relative overflow-hidden rounded-3xl p-8 sm:p-10 bg-gradient-to-br from-brand-blue-900 via-brand-blue-800 to-brand-blue-700`, eyebrow `text-xs font-semibold uppercase tracking-widest text-white/60 mb-6`, grille `grid grid-cols-2 gap-4`, tuile `rounded-2xl border border-white/10 bg-white/10 backdrop-blur-sm p-6 text-center`, valeur `text-4xl font-bold tabular-nums text-white leading-none mb-1.5`, libellé `text-sm text-white/70` ; compteur animé repris de `FE/components/section/Stats.vue` (IntersectionObserver seuil 0,3, easeOutQuart 1 500 ms, décalage 150 ms, suffixe non numérique réaffiché) **uniquement si** `isNumericStat(value)` et hors `prefers-reduced-motion`, sinon valeur brute.
- [X] T023 [P] [US1] Créer `FE/components/entrepreneurship/ProgramCard.vue` (`<EntrepreneurshipProgramCard>`) : props `program: PeiProgramPublic`, `index: number`, `to?: string` ; `const c = peiColorClasses(program.color)` ; racine `NuxtLink` (si `to`) ou `div` `flex flex-col gap-3 rounded-xl border-2 border-gray-200 dark:border-gray-700 border-t-4 bg-white dark:bg-gray-800 p-6 h-full` + `c.border` (+ `hover:shadow-lg transition` si lien) ; ligne haute `flex items-center justify-between` : pastille `w-10 h-10 rounded-xl flex items-center justify-center text-sm font-bold` + `c.numBg c.numText` affichant `index`, libellé `text-xs font-semibold uppercase tracking-wider` + `c.label` = `t('pei.phases.' + program.phase)` ; titre `text-[17px] font-bold leading-snug text-gray-900 dark:text-white` = `localized(program, 'title')` (+ ` (${sigle})` si `sigle` et non déjà contenu) ; accroche `text-sm leading-relaxed text-gray-600 dark:text-gray-300` = `localized(program, 'tagline')` ; chiffre `text-sm font-semibold` + `c.label` = `localized(program, 'highlight')` (masqué si vide).
- [X] T024 [P] [US1] Créer `FE/components/entrepreneurship/EcosystemChips.vue` (`<EntrepreneurshipEcosystemChips>`) : props `title: string`, `items: string[]` ; titre `text-xs font-semibold uppercase tracking-widest text-gray-500 dark:text-gray-400 text-center mb-4`, liste `flex flex-wrap justify-center gap-3`, chip `inline-flex items-center gap-2 rounded-full border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-800 px-4 py-2 text-sm text-gray-700 dark:text-gray-200` avec `fa-solid fa-bolt` `text-brand-blue-700 dark:text-brand-blue-300` ; rien rendu si `items` vide.
- [X] T025 [P] [US1] Créer `FE/components/entrepreneurship/QuoteBlock.vue` (`<EntrepreneurshipQuoteBlock>`) : props `quote: string`, `author?: string`, `role?: string`, `authorImage?: string | null`, `impactText?: string`, `impactImage?: string | null` ; `<section class="relative overflow-hidden bg-gradient-to-br from-brand-blue-900 via-brand-blue-800 to-brand-blue-950 py-24">`, conteneur `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 grid gap-16 lg:grid-cols-12 items-center`, colonne texte `lg:col-span-7` : `fa-solid fa-quote-left` `text-brand-red-500 text-3xl`, `<blockquote class="text-2xl font-semibold leading-snug text-white">« {{ quote }} »</blockquote>`, auteur (`<img class="w-12 h-12 rounded-full object-cover">` ou initiale dans un rond `bg-white/20`, nom `font-semibold text-white`, rôle `text-sm text-white/70`), impact `text-white/80 leading-relaxed` (si `impactText`) ; colonne visuel `lg:col-span-5` `<img class="w-full rounded-2xl aspect-[4/3] object-cover" loading="lazy">` masquée si `impactImage` nul.
- [X] T026 [P] [US1] Créer `FE/components/entrepreneurship/PartnerFamilies.vue` (`<EntrepreneurshipPartnerFamilies>`) : prop `families: PeiPartnerFamilyPublic[]` ; `visible = computed(() => PEI_FAMILY_ORDER.map(f => families.find(x => x.family === f)).filter(f => f && f.partners.length))` ; grille `grid gap-6 md:grid-cols-3`, carte `flex flex-col gap-4 rounded-xl border-2 border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-800 p-6`, badge `self-start rounded-full px-4 py-1.5 text-xs font-semibold uppercase tracking-wider` (academic `bg-brand-blue-100 text-brand-blue-700 dark:bg-brand-blue-900/40 dark:text-brand-blue-300`, support `bg-brand-red-100 text-brand-red-700 dark:bg-brand-red-900/40 dark:text-brand-red-300`, international `bg-teal-100 text-teal-800 dark:bg-teal-900/40 dark:text-teal-300`) = `t('pei.families.' + family)` ; logos `grid grid-cols-2 gap-3`, tuile `h-[72px] rounded-xl border border-gray-200 dark:border-gray-700 bg-white flex items-center justify-center p-3` contenant `<img loading="lazy" :alt="partner.name" class="max-h-12 max-w-full object-contain">` si `logo_url` sinon `<span class="text-sm font-bold text-gray-700 text-center">{{ name }}</span>`, enveloppée d'`<a :href="website" target="_blank" rel="noopener noreferrer">` si `website` ; rien rendu si `visible` vide.
- [X] T027 [P] [US1] Créer `FE/components/entrepreneurship/CtaBanner.vue` (`<EntrepreneurshipCtaBanner>`) : props `title: string`, `description?: string`, `buttonLabel: string`, `to: string`, `email?: string` ; `<div class="rounded-3xl bg-brand-blue-50 dark:bg-brand-blue-900/30 px-6 py-12 sm:p-16 text-center">`, titre `text-3xl font-bold text-gray-900 dark:text-white`, description `mt-4 text-lg text-gray-600 dark:text-gray-300 max-w-2xl mx-auto`, ligne d'actions `mt-8 flex flex-wrap items-center justify-center gap-6` : `NuxtLink :to="localePath(to)"` `inline-flex items-center gap-3 rounded-full bg-brand-blue-500 hover:bg-brand-blue-600 px-8 py-4 font-semibold text-white` + `fa-solid fa-arrow-right rtl:rotate-180`, `<a :href="'mailto:' + email">` `inline-flex items-center gap-2 text-brand-blue-700 dark:text-brand-blue-300 font-medium` + `fa-regular fa-envelope` (si `email`).

### Page d'accueil

- [X] T028 [US1] Créer `FE/pages/entrepreneuriat/index.vue` — script : `useI18n`, `useLocalePath`, `useRoute`, `useRuntimeConfig().public.siteUrl`, `useEditorialContent('entrepreneurship')` (`loadContent`, `getRawContent`, `getHtmlContent`), `useMediaApi().getMediaUrl`, `useLocalizedField`, `usePublicEntrepreneurshipApi`, `usePublicNewsApi().getAllPublishedNews`, `usePublicOrganizationApi()` (`getServiceById`, `slugify`) ; `await useAsyncData('editorial-entrepreneurship', () => loadContent())` ; `ddeServiceId = computed(() => UUID valide ? getRawContent('entrepreneurship.dde_service_id') : null)` ; `useAsyncData('pei-home-programs', () => listPrograms().catch(() => []))`, `'pei-home-partners'` (idem `[]`), `'pei-home-dde'` (`ddeServiceId ? getServiceById(id).catch(() => null) : null`), `'pei-home-news'` (`ddeServiceId ? getAllPublishedNews({ service_id, limit: 3 }).catch(() => []) : []`) ; computed : `slides` (3 clés `hero.slideN.image` → `getMediaUrl(id, 'medium')` filtrées), `stats` (4 paires filtrées sur `value` non vide), `chips` (split `\n`), `ddeLink` (`/a-propos/organisation/service/${slugify(dde.name)}` si résolu), `breadcrumb` (5 niveaux : `nav.home` `/`, `nav.about` `/a-propos`, `about.tabs.organization` `/a-propos/organisation`, `dde.sigle ?? t('pei.breadcrumb.dde')` avec `to: ddeLink` seulement si résolu, `t('pei.breadcrumb.pole')` sans `to`), `quoteImage` / `impactImage` (`getMediaUrl(id, 'medium')`), `email`.
- [X] T029 [US1] Compléter le template de `FE/pages/entrepreneuriat/index.vue` dans l'ordre de la maquette : `<PageHero :title="slogan" :subtitle="hero.subtitle" :badge="hero.badge" :images="slides" :actions="heroActions" :breadcrumb="breadcrumb" />` (prop `actions` livrée en T030 ; repli `:image="'/images/bg/backgroud_senghor2.jpg'"` si `slides` vide) — le slogan `entrepreneurship.hero.slogan` est le `h1`, le badge lit `entrepreneurship.hero.badge` (valeur seedée « Pôle Entrepreneuriat et Innovation », modifiable en backoffice ; `hero.title` sert au `<title>` et au JSON-LD), `heroActions = [{ label: cta1, to: '/entrepreneuriat/statut-etudiant-entrepreneur', variant: 'red' }, { label: cta2, to: '#parcours', variant: 'ghost' }]` ; puis `<EntrepreneurshipSubNav />` ; section présentation `id="presentation"` (`py-24`, grille `lg:grid-cols-2 gap-20 items-center`, eyebrow `presentation.badge`, `h2 text-4xl font-bold` `presentation.title` avec soulignement `w-1/3 h-1 bg-gradient-to-r from-brand-blue-500 to-brand-blue-300` comme `/actualites`, `<RichTextRenderer :html="getHtmlContent('entrepreneurship.presentation.content')" />`, lien `presentation.link` → `ddeLink` avec `fa-arrow-right rtl:rotate-180` masqué si non résolu ; `<EntrepreneurshipStatsPanel v-if="stats.length" :title="stats.title" :stats="stats" />`) ; section parcours `id="parcours"` `bg-gray-50 dark:bg-gray-900/50` (`v-if="programs.length"` : eyebrow, titre, sous-titre centrés, grille `grid gap-5 md:grid-cols-2 xl:grid-cols-5` de `<EntrepreneurshipProgramCard :program :index="i + 1" :to="'/entrepreneuriat/activites#' + PEI_PHASE_ANCHORS[p.phase]" />`, `<EntrepreneurshipEcosystemChips :title :items="chips" />`, bouton `activities.link` → `/entrepreneuriat/activites` `rounded-full border-2 border-brand-blue-200 bg-white text-brand-blue-700 px-8 py-4 font-semibold`) ; `<EntrepreneurshipQuoteBlock v-if="quoteText" … />` ; section actualités (`v-if="news.length"`, en-tête `flex justify-between` : eyebrow `pei.home.newsBadge` + titre `pei.home.ourNews`, lien `pei.home.allNews` → `/entrepreneuriat/actualites` ; grille `grid gap-8 md:grid-cols-3` de `<ActualitesNewsCard :item :show-associations="false" />`) ; section partenaires (`v-if="partnersVisible"`, eyebrow `pei.home.partnersBadge`, titre `pei.home.ourPartners`, `<EntrepreneurshipPartnerFamilies :families />`) ; `<EntrepreneurshipCtaBanner v-if="cta.title" :title :description :button-label="cta.button" to="/entrepreneuriat/statut-etudiant-entrepreneur" :email />` dans un conteneur `py-24`.
- [X] T030 [US1] **À faire avant T029.** Ajouter la prop `actions` à `FE/components/page/Hero.vue` (`actions?: { label: string; to: string; variant?: 'red' | 'ghost'; icon?: string }[]`, rendue `v-if="actions?.length"` après le sous-titre : `flex flex-wrap gap-4 mt-8`, rouge `rounded-full bg-brand-red-500 hover:bg-brand-red-600 px-8 py-4 font-semibold text-white`, fantôme `rounded-full border border-white/30 bg-white/15 px-8 py-4 font-semibold text-white hover:bg-white/25` ; un `to` commençant par `#` rend un `<a href>` avec défilement doux, sinon `NuxtLink localePath`) (déjà décrite dans `specs/023-pei-public-home-activities/contracts/frontend.md`) ; vérifier que les 19 usages existants restent inchangés (aucun ne passe `actions`).
- [X] T031 [US1] Valider `FE/pages/entrepreneuriat/index.vue` selon quickstart § 3–4 : préparer les données en backoffice (3 images de slider, photo / visuel de l'encadré, ≥ 4 actualités DDE publiées, partenaires dans deux familles), ouvrir `/entrepreneuriat`, vérifier chaque section, le `curl … | grep -c "INNOVER"`, les liens du fil d'Ariane, le défilement vers `#parcours`, le lien d'une carte vers `/entrepreneuriat/activites#phase-…`, puis la mise à jour après modification d'un chiffre clé et d'un titre de dispositif ; corriger jusqu'à conformité. Vérifier aussi quickstart § 9 (vider `dde_service_id`, retirer tous les partenaires, désactiver tous les dispositifs, arrêter le backend) : sections masquées, aucune erreur.

**Checkpoint**: l'accueil est livrable seul (MVP) ; la sous-nav pointe vers des rubriques encore « introuvables », comportement accepté.

---

## Phase 4: User Story 2 — Explorer le parcours en détail sur « Nos activités » (Priority: P2)

**Goal**: `/entrepreneuriat/activites` rend un bloc par phase avec le contenu riche des dispositifs, des ancres stables et les prochains événements de la DDE.

**Independent Test**: quickstart § 5 : 5 blocs de phase + bloc écosystème, contenu riche rendu, visuels, événements à venir triés (passé exclu), ancres depuis la barre et par adresse directe (`#phase-incubation`), liste masquée sans événement.

- [X] T032 [P] [US2] Créer `FE/components/entrepreneurship/ProgramSection.vue` (`<EntrepreneurshipProgramSection>`) : props `phase: PeiProgramPhase`, `programs: PeiProgramPublic[]`, `index: number` ; `<section :id="PEI_PHASE_ANCHORS[phase]" class="scroll-mt-32 py-16 border-t border-gray-200 dark:border-gray-700 first:border-t-0">` ; en-tête `flex items-center gap-4 mb-8` : pastille numéro (classes de `ProgramCard`, couleur du premier dispositif), `h2 text-3xl font-bold` = `t('pei.phases.' + phase)` ; par dispositif `<article class="grid gap-8 lg:grid-cols-12 items-start mb-12 last:mb-0">` : visuel `lg:col-span-5` `<img :src="cover_image_url + '?variant=medium'" class="w-full rounded-2xl aspect-[16/10] object-cover" loading="lazy">` (via `getImageVariantUrl`, masqué si nul → texte en pleine largeur), texte `lg:col-span-7` : `h3 text-2xl font-bold` (titre localisé + sigle), accroche `text-lg text-gray-600 dark:text-gray-300`, chiffre `font-semibold` + `c.label`, `<RichTextRenderer :html="localized(program, 'content_html')" class="mt-4" />` (masqué si vide).
- [X] T033 [P] [US2] Créer `FE/components/entrepreneurship/EventList.vue` (`<EntrepreneurshipEventList>`) : prop `events: EventPublic[]` ; titre `h3 text-xl font-bold mb-4` = `t('pei.activities.upcomingEvents')` ; `<ul class="divide-y divide-gray-200 dark:divide-gray-700 rounded-xl border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-800">` ; item `flex flex-wrap items-center gap-4 p-4` : date `inline-flex items-center gap-2 text-sm font-semibold text-brand-blue-700 dark:text-brand-blue-300` (`fa-regular fa-calendar`, `toLocaleDateString` locale `ar-EG` / `en-US` / `fr-FR`, `start_date` + ` – ` + `end_date` si différent), titre `NuxtLink :to="localePath('/actualites/evenements/' + slug)"` `font-medium hover:text-brand-blue-600` = `localized(event, 'title')`, lieu `text-sm text-gray-500` (`venue` / `city` joints par `, ` ou `t('pei.activities.online')` si `is_online`) ; rien rendu si vide.
- [X] T034 [US2] Créer `FE/pages/entrepreneuriat/activites.vue` — script : mêmes composables que l'accueil ; `await useAsyncData('editorial-entrepreneurship', loadContent)` (même clé, cache partagé), `'pei-activities-programs'` (`listPrograms().catch(() => [])`), `'pei-activities-dde'`, `'pei-activities-events'` (`ddeServiceId ? listPublishedEvents({ service_id, upcoming: true, order: 'asc', limit: 6 }).then(r => r.items).catch(() => []) : []`) ; `sections = computed(() => PEI_PHASE_ORDER.filter(ph => ph !== 'ecosystem').map(ph => ({ phase: ph, programs: programs.filter(p => p.phase === ph).sort(display_order) })).filter(s => s.programs.length))` ; `ecosystemPrograms` (dispositifs de phase `ecosystem`, éventuellement vides), `showEcosystem = chips.length || events.length || ecosystemPrograms.length` ; `heroImage = getMediaUrl(getRawContent('entrepreneurship.activities.hero.image'), 'medium')` ; `breadcrumb` = celui de l'accueil + `{ label: t('pei.breadcrumb.pole'), to: '/entrepreneuriat' }` + `{ label: t('pei.nav.activities') }`.
- [X] T035 [US2] Compléter le template de `FE/pages/entrepreneuriat/activites.vue` : `<PageHero :title="activities.hero.title ?? t('pei.seo.activitiesTitle')" :subtitle="activities.hero.subtitle" :badge="activities.hero.badge" :image="heroImage ?? undefined" :breadcrumb="breadcrumb" />` (motif si pas d'image) ; `<EntrepreneurshipSubNav />` ; barre d'ancres non collante `<nav :aria-label="t('pei.activities.anchorsLabel')" class="border-b border-gray-200 dark:border-gray-700 bg-white/60 dark:bg-gray-900/60 backdrop-blur-sm">` > conteneur `flex gap-2 overflow-x-auto scrollbar-hide py-3` de pilules `<a :href="'#' + anchor" class="shrink-0 rounded-full px-4 py-2 text-sm font-medium text-gray-600 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-800 whitespace-nowrap">` (`n. Libellé de phase`) pour chaque section présente + écosystème si `showEcosystem` ; `<main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">` avec `<EntrepreneurshipProgramSection v-for="(s, i) in sections" :key="s.phase" :phase="s.phase" :programs="s.programs" :index="i + 1" />` puis `<section v-if="showEcosystem" :id="PEI_PHASE_ANCHORS.ecosystem" class="scroll-mt-32 py-16 border-t …">` : en-tête (pastille + `t('pei.phases.ecosystem')`), `<EntrepreneurshipProgramSection v-if="ecosystemPrograms.length" phase="ecosystem" :programs="ecosystemPrograms" :index="sections.length + 1" />`, `<EntrepreneurshipEcosystemChips :title="ecosystem.title" :items="chips" />`, `<EntrepreneurshipEventList :events />` ; conserver `scroll-behavior: smooth` global (`base.css`).
- [X] T036 [US2] Valider `FE/pages/entrepreneuriat/activites.vue` selon quickstart § 5 : 2 événements à venir + 1 passé rattachés à la DDE en backoffice ; blocs dans l'ordre, contenu riche (titres, listes, liens) rendu, visuels, tri croissant, passé absent ; ancre par la barre et par adresse directe `/entrepreneuriat/activites#phase-incubation` positionnée sous la sous-nav ; dépublier les événements → liste masquée sans erreur ; corriger jusqu'à conformité.

**Checkpoint**: les deux pages sont livrables ; il reste à garantir l'accessibilité fine, les langues, le mobile et le SEO.

---

## Phase 5: User Story 3 — Naviguer entre les rubriques et parcourir le slider (Priority: P2)

**Goal**: sous-navigation collante et correcte sur les deux pages, slider entièrement pilotable au clavier, en pause au survol / focus, sans défilement automatique sous `prefers-reduced-motion`, dégradant proprement à 1 ou 0 image.

**Independent Test**: quickstart § 6 + § 7 : rubrique active exacte sur les deux pages ; barre visible après 1 500 px de défilement, juste sous l'en-tête ; à 390 px la barre défile horizontalement ; clavier seul (Tab, Entrée / Espace, ← → Home End, annonce « Visuel 2 sur 3 ») ; pause au survol, reprise à la sortie ; « Réduire les animations » → aucun changement automatique en 60 s ; 2 images → slider, 1 → image simple, 0 → motif ; captures des 19 usages de `PageHero` identiques.

- [X] T037 [US3] Vérifier et corriger `FE/components/entrepreneurship/SubNav.vue` sur les deux pages : « Présentation » active uniquement sur `/entrepreneuriat` (exact), « Nos activités » active sur `/entrepreneuriat/activites`, `aria-current="page"`, position `top-20` (en-tête `AppNavBar` = 5 rem) alignée sans chevauchement ni jour, comparée à `/a-propos/organisation` à 1440 px et 390 px, défilement horizontal à 390 px sans barre de défilement visible, bouton toujours atteignable (dernier élément défilable), contraste des onglets en mode sombre.
- [X] T038 [US3] Vérifier et corriger le slider de `FE/components/page/Hero.vue` au clavier (Tab atteint le point actif, Entrée / Espace / ← → / Home / End fonctionnent, flèches inversées en RTL, focus visible sur fond sombre), la pause au survol et au focus (`mouseenter` / `focusin` stoppent l'intervalle, reprise à la sortie), la pause sur `document.hidden`, l'absence d'intervalle et de transition sous `prefers-reduced-motion` (macOS → Réglages → Accessibilité → Réduire les animations, ou DevTools → Rendering → Emulate CSS media feature), l'annonce `aria-live`, et les cas 0 / 1 / 2 / 3 images (retirer des images en backoffice) ; vérifier qu'une image dont le média a été supprimé (`getMediaUrl` → URL 404) est filtrée après échec de chargement (`@error` retire la slide) sans erreur console.
- [ ] T039 [US3] Captures avant / après (1440 px et 390 px) des 17 pages listées dans quickstart § 7 utilisant `PageHero` (dont `/a-propos/partenaires` et une fiche service, nettoyées en T014) : diff visuel nul ; documenter le résultat dans `specs/023-pei-public-home-activities/quickstart.md` § 7 (date, pages vérifiées). — **Partiel (2026-09-13)** : DOM normalisé identique sur 11 pages + `/actualites` à 1440 px (quickstart § 7) ; fiches formation / projet / appel et 390 px non comparés.

---

## Phase 6: User Story 4 — Trilingue, RTL, mobile, sombre et référencement (Priority: P3)

**Goal**: pages complètes en FR / EN / AR (libellés fixes traduits, copie éditoriale FR par convention, données métier localisées avec repli), miroir RTL, 390 px sans débordement, mode sombre propre, balises de partage, sitemap et JSON-LD valides.

**Independent Test**: quickstart § 8, § 10, § 11 : `/en/…` et `/ar/…` sans clé brute ni libellé fixe non traduit, `dir="rtl"` et mise en page miroir ; `scrollWidth === innerWidth` à 390 px ; captures sombres ; `og:*` présents, ≥ 5 scripts JSON-LD valides sur validator.schema.org, 6 entrées `entrepreneuriat` dans `sitemap.xml` ; Lighthouse mobile ≥ 90 / ≥ 90.

- [X] T040 [US4] Ajouter le SEO à `FE/pages/entrepreneuriat/index.vue` **après** `useRoute()` et les `useAsyncData` (gotcha TDZ) : `const localeMap = { fr: 'fr_FR', en: 'en_US', ar: 'ar_SA' }` ; `useSeoMeta({ title: () => hero.title ?? t('pei.seo.homeTitle'), description: () => hero.subtitle ?? t('pei.seo.homeDescription'), ogTitle, ogDescription, ogUrl: () => siteUrl + route.fullPath, ogImage: () => slides[0] ? siteUrl + slides[0] : undefined, ogLocale: () => localeMap[locale] ?? 'fr_FR', ogLocaleAlternate: () => autres })` ; créer d'abord `FE/composables/usePeiJsonLd.ts` (`buildPeiOrganization(...)`, `buildWebPage(...)`, `buildBreadcrumbList(...)`) puis `useHead(() => ({ script: [ { type: 'application/ld+json', key: 'jsonld-pei-organization', innerHTML: JSON.stringify(orgJsonLd) }, { key: 'jsonld-pei-webpage', … }, { key: 'jsonld-pei-breadcrumb', … } ] }))` avec `orgJsonLd` (`@type Organization`, `@id ${siteUrl}/entrepreneuriat#organization`, `name`, `url ${siteUrl}${localePath('/entrepreneuriat')}`, `email`, `parentOrganization { @id ${siteUrl}/#organization }`), `webPageJsonLd` (`@type WebPage`, `@id ${siteUrl}${route.path}#webpage`, `name`, `description`, `inLanguage`, `isPartOf { @id ${siteUrl}/#website }`, `about { @id …#organization }`), `breadcrumbJsonLd` (`@type BreadcrumbList`, `itemListElement` depuis `breadcrumb` : `position`, `name`, `item: siteUrl + localePath(to)` sauf dernier) ; ne pas surcharger `htmlAttrs.dir` (géré par `app.vue`).
- [X] T041 [US4] Ajouter le même SEO à `FE/pages/entrepreneuriat/activites.vue` (titre / description depuis `activities.hero.*` avec repli `pei.seo.activities*`, `ogImage` = image du hero si présente, `WebPage` `@id …/entrepreneuriat/activites#webpage`, `BreadcrumbList` à 6 niveaux, `Organization` identique — même `@id` que l'accueil) en réutilisant `FE/composables/usePeiJsonLd.ts` créé en T040.
- [X] T042 [US4] Vérifier `I18N/en/entrepreneurship.json` et `I18N/ar/entrepreneurship.json` (T005, T006) en contexte : ouvrir `/en/entrepreneuriat`, `/ar/entrepreneuriat`, `/en/entrepreneuriat/activites`, `/ar/entrepreneuriat/activites` ; aucun texte `pei.` ni `entrepreneurship.` visible, libellés de phases et de familles traduits, dates localisées (`ar-EG`, `en-US`), données métier localisées avec repli FR (vider un titre EN en backoffice → FR affiché sans mention) ; corriger les JSON.
- [X] T043 [US4] Vérifier le RTL en arabe sur `FE/pages/entrepreneuriat/{index,activites}.vue` et `FE/components/entrepreneurship/*.vue` : `dir="rtl"` global, sous-nav (bouton à l'extrémité opposée, onglets en miroir), chevrons du fil d'Ariane et flèches (`rtl:rotate-180`), points du slider (← → inversés), cartes du parcours (pastille et libellé inversés), panneau de chiffres, liste d'événements ; remplacer toute classe directionnelle (`ml-`, `mr-`, `pl-`, `pr-`, `left-`, `right-`, `text-left`) restée dans les nouveaux composants par sa propriété logique.
- [X] T044 [US4] Vérifier à 390 px (DevTools, iPhone 12/13) `FE/pages/entrepreneuriat/{index,activites}.vue` et `FE/components/entrepreneurship/*.vue` : sous-nav défilante, cartes du parcours en une colonne, slider pleine largeur avec points visibles, panneau de chiffres 2 × 2 lisible, grilles actualités / partenaires / phases en colonne, `document.documentElement.scrollWidth === window.innerWidth` sur les deux pages ; corriger les débordements (`min-w-0`, `break-words`).
- [X] T045 [US4] Vérifier le mode sombre (bascule `useDarkMode`) sur `FE/pages/entrepreneuriat/{index,activites}.vue` et `FE/components/entrepreneurship/*.vue` : cartes, encadré citation (déjà sombre), panneau de chiffres, chips, tuiles de logos (fond blanc volontaire pour les logos, bordure adaptée), bandeau CTA, barre d'ancres, liste d'événements ; contraste AA des textes ; corriger les classes `dark:` manquantes.
- [X] T046 [US4] Vérifier le référencement de `FE/pages/entrepreneuriat/{index,activites}.vue` (quickstart § 10) : `curl -s http://localhost:3000/entrepreneuriat | grep -oE '<meta property="og:[a-z:_]+" content="[^"]*"'` (title, description, url, image, locale, locale:alternate), `grep -c 'application/ld+json'` ≥ 5, JSON-LD extrait validé sur https://validator.schema.org (aucune erreur), `curl -s http://localhost:3000/sitemap.xml | grep -c entrepreneuriat` = 6, `hreflang` présents (héritage `useLocaleHead`).

---

## Phase 7: Polish & Cross-Cutting Concerns

**Purpose**: qualité de build, performance, documentation.

- [X] T047 Exécuter `cd usenghor_nuxt && pnpm lint` et `pnpm build` ; corriger toute erreur ESLint / TypeScript (types `PeiProgramPhase` dans `PEI_PHASE_ANCHORS`, props optionnelles du hero, imports auto).
- [ ] T048 Mesurer Lighthouse mobile en production locale (quickstart § 11 : `pnpm build && node .output/server/index.mjs`, `npx lighthouse … --form-factor=mobile --only-categories=performance,accessibility`) sur `/entrepreneuriat` et `/entrepreneuriat/activites` avec 3 images de slider ; si < 90, appliquer research R14 (variante `medium` / `low`, `fetchpriority`, dimensions explicites, `loading="lazy"`, `content-visibility`) et re-mesurer ; consigner les scores dans `specs/023-pei-public-home-activities/quickstart.md` § 11. — **Non réalisé (2026-09-13)** : Lighthouse indisponible en local (NO_FCP headless, médias 404 sans nginx) ; à mesurer derrière nginx.
- [X] T049 [P] Exécuter la suite backend complète `cd usenghor_backend && pytest -q` (aucune régression sur `test_public_entrepreneurship_api.py`, `test_public_pei_api.py` et les tests d'événements existants).
- [X] T050 [P] Mettre à jour `CLAUDE.md` : tableau « Composants clés » (+ `PageHero` props `images` / `badge` / `actions`, `EntrepreneurshipSubNav`, `ActualitesNewsCard`, `usePublicEntrepreneurshipApi()`), section « Recent Changes » (entrée `023-pei-public-home-activities` : routes publiques `/entrepreneuriat` et `/entrepreneuriat/activites`, migration `047_pei_activities_hero_keys.sql`, filtre `service_id` / `order` sur `/api/public/events`, namespace i18n `pei`, copie éditoriale FR dans les trois langues), et la ligne « Migrations » si elle liste les numéros.
- [ ] T051 Mise en production (SC-011) : commit + push `origin/main` (les deux sous-dépôts), jouer `SQL/migrations/047_pei_activities_hero_keys.sql` sur `usenghor_db` (`docker exec -i usenghor_db psql -U usenghor -d usenghor < …`, deux fois, vérifier 47 clés), `./deploy.sh update` avec `--force-recreate`, puis contrôler `/entrepreneuriat` et `/entrepreneuriat/activites` en production (SSR, trois langues) ; choisir les images du slider en backoffice de production. — **Non réalisé** : mise en production (push, migration sur `usenghor_db`, déploiement) soumise à confirmation du responsable.
- [X] T052 Relire `specs/023-pei-public-home-activities/spec.md` (SC-001 à SC-011) et cocher mentalement chaque critère avec la preuve (quickstart § 1–11) ; mettre à jour `contracts/frontend.md` si des props ou classes ont changé pendant l'implémentation ; préparer le commit (message décrivant les deux pages, le hero étendu, la carte extraite, la migration 047 et le filtre événements).

---

## Dependencies & Execution Order

### Phase Dependencies

- **Phase 1 (Setup)** : T001 (porte SQL) bloque uniquement T018–T019 ; T002–T006 indépendantes.
- **Phase 2 (Foundational)** : dépend de T003–T006 (utilitaires, i18n) ; bloque toutes les stories. T007 → T008 → T009 (backend en série) ; T010, T011 en parallèle ; T012 → T013 (hero) ; T014 parallèle ; T015 dépend de T002, T004 ; T016 → T017 ; T018 → T019 après T001 ; T020, T021 parallèles.
- **US1 (Phase 3)** : dépend de la phase 2 (hero, sous-nav, carte, composable, stats) ; T022–T027 parallèles ; T030 → T028 → T029 → T031 (la prop `actions` du hero précède la page).
- **US2 (Phase 4)** : dépend de la phase 2 et de T023 (`ProgramCard` pour les classes) ; T032, T033 parallèles ; T034 → T035 → T036. Peut démarrer en parallèle de US1 après T023.
- **US3 (Phase 5)** : validation ; dépend de US1 (page pour tester) et de T012–T015 ; T037, T038 parallèles ; T039 après T014.
- **US4 (Phase 6)** : dépend de US1 et US2 ; T040, T041 parallèles (T041 introduit `usePeiJsonLd`, à faire avant ou avec T040) ; T042–T046 parallèles entre elles après T040–T041.
- **Phase 7** : après tout le reste ; T049, T050 parallèles ; T051 après T047–T050 ; T052 en dernier.

### User Story Dependencies

- **US1 (P1)** : socle → MVP livrable seul.
- **US2 (P2)** : socle + `ProgramCard` ; indépendante de US1 côté page.
- **US3 (P2)** : valide les briques de la phase 2 via la page US1.
- **US4 (P3)** : couche transverse sur US1 + US2.

### Parallel Opportunities

- Phase 1 : T002, T003, T004, T005, T006 ensemble (5 fichiers distincts).
- Phase 2 : {T007→T008→T009} ‖ T010 ‖ T011 ‖ {T012→T013} ‖ T014 ‖ T015 ‖ {T016→T017} ‖ {T018→T019 après T001} ‖ T020 ‖ T021.
- US1 : T022–T027 (6 composants) ensemble, puis T028–T031 en série.
- US2 : T032 ‖ T033, puis T034–T036 ; US2 entière en parallèle de US1 après T023.
- US4 : T042 ‖ T043 ‖ T044 ‖ T045 ‖ T046 après T040–T041.

---

## Parallel Example: User Story 1

```bash
# Après la phase 2, lancer ensemble (fichiers distincts) :
T022 FE/components/entrepreneurship/StatsPanel.vue
T023 FE/components/entrepreneurship/ProgramCard.vue
T024 FE/components/entrepreneurship/EcosystemChips.vue
T025 FE/components/entrepreneurship/QuoteBlock.vue
T026 FE/components/entrepreneurship/PartnerFamilies.vue
T027 FE/components/entrepreneurship/CtaBanner.vue
# puis en série : T030 → T028 → T029 → T031 (prop actions, page index.vue, validation)
```

## Parallel Example: Phase 2

```bash
# Sous-agents en parallèle (max 10) :
A : T007 → T008 → T009   (backend événements + test)
B : T010                  (usePublicEventsApi)
C : T011                  (usePublicEntrepreneurshipApi)
D : T012 → T013           (PageHero slider + a11y)
E : T014                  (attributs orphelins)
F : T015                  (SubNav)
G : T016 → T017           (NewsCard + /actualites)
H : T020, T021            (types + config éditoriale)
I : T018 → T019           (migration 047, après accord T001)
```

---

## Implementation Strategy

### MVP First (US1 seule)

1. Phase 1 (T001 accord en parallèle, T002–T006).
2. Phase 2 complète (T007–T021).
3. Phase 3 (T022–T031) → `/entrepreneuriat` fonctionnelle en SSR avec les données du backoffice.
4. **STOP et valider** quickstart § 4 et § 9 avant d'aller plus loin.

### Incremental Delivery

1. Socle → accueil (US1) → démonstration.
2. « Nos activités » (US2) → démonstration des ancres et événements.
3. Accessibilité / navigation (US3) → captures de non-régression.
4. Langues, RTL, mobile, sombre, SEO (US4) → Lighthouse.
5. Polish (lint, build, pytest, CLAUDE.md) → commit + push (deploy.sh part de GitHub) → migration 047 en production (`docker exec -i usenghor_db …`) → `./deploy.sh update` avec `--force-recreate`.

### Notes

- Toutes les tâches suivent le format `- [ ] Txxx [P?] [USn?] description avec chemin`.
- Les libellés fixes vivent sous `pei.*` (jamais `entrepreneurship.*`, réservé aux clés éditoriales) ; la copie éditoriale reste en français dans les trois langues (clarification Q1).
- Aucune modification de schéma ; la migration 047 n'ajoute que 4 lignes et attend l'accord T001.
- Zéro régression : `PageHero` (19 usages) et `/actualites` sont comparés par captures avant / après.
